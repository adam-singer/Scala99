import Scala99._
import com.scalakata.eval._
@ScalaKata object P07 {
  val prob = problem(7, Medium, "Flatten a nested list structure")
  example("flatten(List(List(1, 1), 2, List(3, List(5, 8)))) = List(1, 1, 2, 3, 5, 8)")
}
